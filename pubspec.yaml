name: curl_logger_dio_interceptor
description: Intercept dio requests and print them as curl requests
version: 0.0.4
homepage: https://github.com/LastxTemplar/curl_logger_dio_interceptor_maintained

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  dio: ^5.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
